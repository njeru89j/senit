<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Password Reset Request - SendIT</title>
    <!-- Debug: name="<%= typeof name !== 'undefined' ? name : 'UNDEFINED' %>", profilePicture="<%= typeof profilePicture !== 'undefined' ? profilePicture : 'UNDEFINED' %>" -->
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            color: #333;
            line-height: 1.6;
            min-height: 100vh;
        }
        
        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        
        .header {
            text-align: center;
            margin-bottom: 30px;
        }
        
        .logo {
            width: 120px;
            height: auto;
            margin-bottom: 20px;
        }
        
        .main-heading {
            font-size: 24px;
            font-weight: 600;
            color: #333;
            margin-bottom: 10px;
        }
        
        .content-card {
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            padding: 40px;
            margin: 0 auto;
        }
        
        .user-avatar {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            margin: 0 auto 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: #DBBB02;
            color: #fff;
            font-size: 32px;
            font-weight: bold;
            position: relative;
            overflow: hidden;
        }
        
        .user-avatar img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            border-radius: 50%;
        }
        
        .user-avatar .initial {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-size: 32px;
            font-weight: bold;
            color: #fff;
            z-index: 1;
        }
        
        .card-heading {
            font-size: 20px;
            font-weight: 600;
            color: #333;
            text-align: center;
            margin-bottom: 15px;
        }
        
        .greeting {
            text-align: center;
            font-size: 16px;
            color: #333;
            margin-bottom: 25px;
        }
        
        .content-text {
            font-size: 14px;
            color: #333;
            margin-bottom: 15px;
            line-height: 1.6;
        }
        
        .highlight-text {
            font-weight: 600;
            text-align: center;
            margin: 20px 0;
            color: #DBBB02;
            font-size: 16px;
            background-color: #fef7e0;
            padding: 15px;
            border-radius: 6px;
            border-left: 4px solid #DBBB02;
        }
        
        .token-section {
            text-align: center;
            margin: 30px 0;
            padding: 25px;
            background: linear-gradient(135deg, #fef7e0 0%, #fff3cd 100%);
            border-radius: 12px;
            border: 2px solid #DBBB02;
        }
        
        .token-label {
            font-size: 18px;
            font-weight: 600;
            color: #333;
            margin-bottom: 20px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .token-display {
            display: flex;
            justify-content: center;
            gap: 8px;
            margin: 20px 0;
        }
        
        .token-digit {
            display: inline-block;
            width: 50px;
            height: 60px;
            background-color: #DBBB02;
            color: #fff;
            font-size: 24px;
            font-weight: bold;
            text-align: center;
            line-height: 60px;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(219, 187, 2, 0.3);
            border: 2px solid #c4a902;
        }
        
        .token-instruction {
            font-size: 14px;
            color: #666;
            margin-top: 15px;
            font-style: italic;
        }
        
        .security-notice {
            background-color: #fff3cd;
            border: 1px solid #ffeaa7;
            border-radius: 6px;
            padding: 15px;
            margin: 20px 0;
            font-size: 13px;
            color: #856404;
        }
        
        .cta-button {
            display: inline-block;
            background-color: #DBBB02;
            color: #fff;
            text-decoration: none;
            padding: 12px 24px;
            border-radius: 6px;
            font-weight: 600;
            font-size: 14px;
            text-align: center;
            margin: 20px 0;
            border: none;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        
        .cta-button:hover {
            background-color: #c4a902;
        }
        
        .support-link {
            text-align: center;
            margin: 20px 0;
        }
        
        .support-link a {
            color: #DBBB02;
            text-decoration: none;
            font-weight: 600;
        }
        
        .support-link a:hover {
            text-decoration: underline;
        }
        
        .separator {
            border-top: 1px solid #e1e4e8;
            margin: 25px 0;
        }
        
        .fallback-link {
            font-size: 12px;
            color: #666;
            margin-top: 15px;
            background-color: #f8f9fa;
            padding: 15px;
            border-radius: 6px;
        }
        
        .fallback-link a {
            color: #DBBB02;
            text-decoration: none;
            word-break: break-all;
        }
        
        .footer {
            text-align: center;
            margin-top: 30px;
        }
        
        .footer a {
            color: #DBBB02;
            text-decoration: none;
            font-size: 14px;
        }
        
        .footer a:hover {
            text-decoration: underline;
        }
        
        .footer .separator-dot {
            color: #333;
            margin: 0 8px;
        }
        
        @media (max-width: 600px) {
            .container {
                padding: 10px;
            }
            
            .content-card {
                padding: 20px;
            }
            
            .main-heading {
                font-size: 20px;
            }
            
            .card-heading {
                font-size: 18px;
            }
            
            .logo {
                width: 100px;
            }
            
            .token-display {
                gap: 4px;
            }
            
            .token-digit {
                width: 40px;
                height: 50px;
                font-size: 20px;
                line-height: 50px;
            }
            
            .token-label {
                font-size: 16px;
            }
        }
    </style>
</head>
<body>
    <div class="container">

        <!-- Main Content Card -->
        <div class="content-card">
            <div style="text-align: center; margin: 0;">
                <img src="https://res.cloudinary.com/duymwzfhj/image/upload/v1753344984/1logo-removebg-preview_inqcyd.png" alt="" style="width: 150px; height: 150px; object-fit: contain;" >
            </div>
            <div class="user-avatar">
                <!-- Debug: name="<%= typeof name !== 'undefined' ? name : 'UNDEFINED' %>", profilePicture="<%= typeof profilePicture !== 'undefined' ? profilePicture : 'UNDEFINED' %>" -->
                <% if (profilePicture && profilePicture.trim() !== '' && profilePicture !== 'undefined' && profilePicture !== 'null') { %>
                    <img src="<%= profilePicture %>" alt="<%= name %>'s profile">
                <% } else { %>
                    <span class="initial"><%= name ? name.charAt(0).toUpperCase() : 'U' %></span>
                <% } %>
            </div>
            
            <h2 class="card-heading">Password Reset Request</h2>
            <div class="separator"></div>
            
            <div class="greeting">
                <!-- Debug: name="<%= typeof name !== 'undefined' ? name : 'UNDEFINED' %>" -->
                Greetings, <strong><%= name %></strong>
            </div>
            
            <div class="content-text">
                We received a request to reset your password for your account. If you didn't make this request, you can ignore this email.
            </div>
            
            <div class="token-section">
                <h3 class="token-label">Your 6-Digit Reset Code</h3>
                <div class="token-display">
                    <span class="token-digit"><%= typeof resetToken !== 'undefined' ? resetToken.charAt(0) : '1' %></span>
                    <span class="token-digit"><%= typeof resetToken !== 'undefined' ? resetToken.charAt(1) : '2' %></span>
                    <span class="token-digit"><%= typeof resetToken !== 'undefined' ? resetToken.charAt(2) : '3' %></span>
                    <span class="token-digit"><%= typeof resetToken !== 'undefined' ? resetToken.charAt(3) : '4' %></span>
                    <span class="token-digit"><%= typeof resetToken !== 'undefined' ? resetToken.charAt(4) : '5' %></span>
                    <span class="token-digit"><%= typeof resetToken !== 'undefined' ? resetToken.charAt(5) : '6' %></span>
                </div>
                <p class="token-instruction">Enter this code in the password reset form to verify your identity.</p>
            </div>
            
            <div class="content-text">
                If you requested a password reset, click the button below to create a new password. For your security, this link will only work once and expires in 1 hour.
            </div>
            
            <div style="text-align: center;">
                <a href="<%= typeof baseUrl !== 'undefined' ? baseUrl : 'http://localhost:4200' %>/reset-password?token=<%= typeof resetToken !== 'undefined' ? resetToken : '123456' %>" class="cta-button">
                    Reset Password
                </a>
            </div>
            
            
            <div class="support-link">
                If you didn't request this password reset, please <a href="<%= typeof baseUrl !== 'undefined' ? baseUrl : 'http://localhost:4200' %>/contact">contact our support team</a> immediately.
            </div>
            
            <div class="separator"></div>
            
        </div>

        <!-- Footer -->
        <div class="footer">
            <a href="<%= typeof baseUrl !== 'undefined' ? baseUrl : 'http://localhost:4200' %>/help">Help Center</a>
            <span class="separator-dot">•</span>
            <a href="<%= typeof baseUrl !== 'undefined' ? baseUrl : 'http://localhost:4200' %>/contact">Contact us</a>
        </div>
    </div>
</body>
</html> 