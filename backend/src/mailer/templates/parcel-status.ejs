<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Parcel Status Update - SendIT</title>
    <!-- Debug: name="<%= typeof name !== 'undefined' ? name : 'UNDEFINED' %>", profilePicture="<%= typeof profilePicture !== 'undefined' ? profilePicture : 'UNDEFINED' %>" -->
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            color: #333;
            line-height: 1.6;
            min-height: 100vh;
        }
        
        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        
        .header {
            text-align: center;
            margin-bottom: 30px;
        }
        
        .logo {
            width: 120px;
            height: auto;
            margin-bottom: 20px;
        }
        
        .main-heading {
            font-size: 24px;
            font-weight: 600;
            color: #333;
            margin-bottom: 10px;
        }
        
        .content-card {
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            padding: 40px;
            margin-bottom: 30px;
        }
        
        .user-avatar {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            margin: 0 auto 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: #DBBB02;
            color: #fff;
            font-size: 32px;
            font-weight: bold;
            position: relative;
            overflow: hidden;
        }
        
        .user-avatar img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            border-radius: 50%;
        }
        
        .user-avatar .initial {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-size: 32px;
            font-weight: bold;
            color: #fff;
            z-index: 1;
        }
        
        .card-heading {
            font-size: 20px;
            font-weight: 600;
            color: #333;
            text-align: center;
            margin-bottom: 15px;
        }
        
        .greeting {
            text-align: center;
            font-size: 16px;
            color: #333;
            margin-bottom: 25px;
        }
        
        .content-text {
            font-size: 14px;
            color: #333;
            margin-bottom: 15px;
            line-height: 1.6;
        }
        .separator {
            border-top: 1px solid #e1e4e8;
            margin: 25px 0;
        }
        
        .parcel-details {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 6px;
            margin: 20px 0;
            border-left: 4px solid #DBBB02;
        }
        
        .detail-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 10px;
            padding: 8px 0;
            border-bottom: 1px solid #e9ecef;
        }
        
        .detail-row:last-child {
            border-bottom: none;
            margin-bottom: 0;
        }
        
        .detail-label {
            font-weight: 600;
            color: #333;
            font-size: 14px;
        }
        
        .detail-value {
            color: #666;
            font-size: 14px;
            text-align: right;
        }
        
        .status-badge {
            background-color: #DBBB02;
            color: #fff;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
        }
        
        .highlight-text {
            font-weight: 600;
            text-align: center;
            margin: 20px 0;
            color: #DBBB02;
            font-size: 16px;
        }
        
        .cta-button {
            display: inline-block;
            background-color: #DBBB02;
            color: #fff;
            text-decoration: none;
            padding: 12px 24px;
            border-radius: 6px;
            font-weight: 600;
            font-size: 14px;
            text-align: center;
            margin: 20px 0;
            border: none;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        
        .cta-button:hover {
            background-color: #c4a902;
        }
        
        .support-link {
            text-align: center;
            margin: 20px 0;
        }
        
        .support-link a {
            color: #DBBB02;
            text-decoration: none;
            font-weight: 600;
        }
        
        .support-link a:hover {
            text-decoration: underline;
        }
        
        .footer {
            text-align: center;
            margin-top: 30px;
        }
        
        .footer a {
            color: #DBBB02;
            text-decoration: none;
            font-size: 14px;
        }
        
        .footer a:hover {
            text-decoration: underline;
        }
        
        .footer .separator-dot {
            color: #333;
            margin: 0 8px;
        }
        
        @media (max-width: 600px) {
            .container {
                padding: 10px;
            }
            
            .content-card {
                padding: 20px;
            }
            
            .main-heading {
                font-size: 20px;
            }
            
            .card-heading {
                font-size: 18px;
            }
            
            .detail-row {
                flex-direction: column;
                align-items: flex-start;
            }
            
            .detail-value {
                text-align: left;
                margin-top: 4px;
            }
            
            .logo {
                width: 100px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        

        <!-- Main Content Card -->
        <div class="content-card">
            <div style="text-align: center; margin: 0;">    
                <img src="https://res.cloudinary.com/duymwzfhj/image/upload/v1753344984/1logo-removebg-preview_inqcyd.png" alt="" style="width: 150px; height: 150px; object-fit: contain;" >
            </div>
            <div class="user-avatar">
                <!-- Debug: name="<%= typeof name !== 'undefined' ? name : 'UNDEFINED' %>", profilePicture="<%= typeof profilePicture !== 'undefined' ? profilePicture : 'UNDEFINED' %>" -->
                <% if (profilePicture && profilePicture.trim() !== '' && profilePicture !== 'undefined' && profilePicture !== 'null') { %>
                    <img src="<%= profilePicture %>" alt="<%= name %>'s profile">
                <% } else { %>
                    <span class="initial"><%= name ? name.charAt(0).toUpperCase() : 'U' %></span>
                <% } %>
            </div>
            
            <h2 class="card-heading">Parcel Status Update</h2>
            <div class="separator"></div>
            
            <div class="greeting">
                <!-- Debug: name="<%= typeof name !== 'undefined' ? name : 'UNDEFINED' %>" -->
                Greetings, <strong><%= name %></strong>
            </div>
            
            <div class="content-text">
                Your parcel has been delivered. Here are the details:
            </div>
            
            <div class="parcel-details">
                <div class="detail-row">
                    <span class="detail-label">Tracking Number:</span>
                    <span class="detail-value"><%= typeof trackingNumber !== 'undefined' ? trackingNumber : 'N/A' %></span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Parcel ID:</span>
                    <span class="detail-value"><%= typeof parcelId !== 'undefined' ? parcelId : 'N/A' %></span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Current Status:</span>
                    <span class="detail-value">
                        <span class="status-badge"><%= typeof status !== 'undefined' ? status : 'Unknown' %></span>
                    </span>
                </div>
                <% if (typeof estimatedDelivery !== 'undefined' && estimatedDelivery) { %>
                <div class="detail-row">
                    <span class="detail-label">Estimated Delivery:</span>
                    <span class="detail-value"><%= typeof estimatedDelivery !== 'undefined' ? estimatedDelivery : 'TBD' %></span>
                </div>
                <% } %>
            </div>
            
            <div class="highlight-text">
                Inspect your parcel and mark as complete, leave a review and rate the driver.
            </div>
            
            
            <div style="text-align: center;">
                <a href="<%= typeof baseUrl !== 'undefined' ? baseUrl : 'http://localhost:4200' %>/track/<%= typeof trackingNumber !== 'undefined' ? trackingNumber : 'N/A' %>" class="cta-button">
                    Track Parcel
                </a>
            </div>
            
            <div class="support-link">
                Have questions about your delivery? <a href="<%= typeof baseUrl !== 'undefined' ? baseUrl : 'http://localhost:4200' %>/contact">Contact our support team</a>.
            </div>
        </div>

        <!-- Footer -->
        <div class="footer">
            <a href="<%= typeof baseUrl !== 'undefined' ? baseUrl : 'http://localhost:4200' %>/help">Help Center</a>
            <span class="separator-dot">•</span>
            <a href="<%= typeof baseUrl !== 'undefined' ? baseUrl : 'http://localhost:4200' %>/contact">Contact us</a>
        </div>
    </div>
</body>
</html> 